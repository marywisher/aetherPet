# CONTEXT.md — aetherPet 领域术语表

> 本文件是领域术语的唯一权威。仅供澄清语言，不包含实现细节。
> 新增/修改术语时，在此处同步更新。

## 术语

### pet（宠物）
用户拥有的陪伴角色。项目中的一切行为都围绕 pet 展开：它有状态、有记忆、会随机触发行为。**一个用户（账号）下可有多个 pet。**

### 账号（account）
MVP 起就存在的用户身份。**注册/登录使用邮箱 + 邮箱验证码**（无密码体系）。一个账号下可有多个 pet（结构一对多；MVP 首发每账号 1 pet，可视作「创建账号即拥有了第一只 pet」）。账号同时是「联系使用者」的通道（邮箱推送，仅在极端必要场景由运营方使用，遵循少打扰纪律）。

## 服务中心（service hub）
承载账号与 pet 数据的自治服务实例。MVP 阶段：官方托管中心 + 用户自托管中心并存。数据模型从第一天按「多中心可迁移」设计。

## 数据主权（data sovereignty）
pet 的记忆与时间线属于用户，不绑定单一中心。用户可随时导出全部数据，也可指定迁移到其它服务中心（未来）。这符合去中心化调性：**数据跟随用户，而非跟随服务商。**

## 隐居模式（hermit mode）
原「单机模式」的更佳命名：**pet 之间互不交互**（≠ 离线、≠ 不联网）。在此模式下，pet 可以与外部服务（如 AI 模型 API）通信，但不与任何其他用户的 pet 交换数据。这是 aetherPet 的默认存在形态，也是 MVP 的唯一形态。面向用户的用语即「隐居」——每个 pet 各自过各自的，不社交。

## 退避间隔（backoff interval）
pet 的主动触达（回信、自主冒话）频率随用户活跃度动态退避的机制，参考 QQ 回复间隔：

| 用户缺席时长 | pet 下次主动触达的间隔 |
|--------------|------------------------|
| 每天来 | 正常频率（次日/当晚回信） |
| ≥ 1 天 | 退避到 3 天 |
| ≥ 3 天 | 退避到 7 天 |
| ≥ 7 天 | 退避到 30 天 |
| 更久 | 维持 30 天上限（永不消失，但极低频） |

效果：用户回来越勤，pet 越热闹；回来越少，pet 越安静克制。与补算、聚合摘要共同保证「长期离线的回归体验不轰击用户」。

## 公告（announcement）
服务中心对使用者的低频触达通道（版本动态、大事记、迁移通知）。pet 每次联网同步时拉取并展示。这是未来「去中心化后仍能联系所有使用者」的基础设施之一。

## 联邦广播（federation broadcast）
未来形态：通过目录/注册表发现所有服务中心，向各中心广播公告，由各中心决定是否展示给其用户（类似 Mastodon relay 模式）。MVP 不做，但公告协议预留兼容空间。

### 去中心化
未来形态：多个 pet 节点可组网交互（类似 Mastodon）。实现前提是节点协议，与「隐居运行」不冲突——隐居是常态，组网是可选附加。**MVP 明确不做组网。**

### 补算（catch-up simulation）
当用户不使用服务时，pet 的世界不推进、不产生事件。**补算发生在服务端：用户下次发起同步请求时**，根据上次活动结束时间戳与当前时间戳之间的流逝时间，一次性补算并生成错过的随机事件（受硬上限/聚合摘要/退避间隔约束），达到「pet 一直在过日子」的等效体验。

### 常驻进程（resident process）
未来形态：服务持续运行，pet 实时产生事件。仅当具备线上服务器后的形态，与补算互补，不影响去中心化设计。

### 事件引擎（event engine）
时间驱动的随机事件生成器：根据 pet 状态、记忆与流逝时间，低频随机产出事件（外出、带回照片、自言自语……）。这是产品的灵魂组件。事件引擎只产出「事件结构」，不关心表现层（见「事件」）。

## 表现素材包（theme/voice pack）
视觉主题与文案基调的统一可配置单元。**不同服务中心可加载不同素材包**（风格定制）；同一中心内也可切换。这是插件生态的最轻入口：不写代码，只替换素材/文案文件即可定制表现层，与事件引擎完全解耦。MVP 内置官方默认素材包（手绘暖调 + 废话/诗意文案）并支持替换。

## 事件（event）
事件引擎产出的最小语义单元，如「去河边看水」「数叶子」「馈赠后的回信」。事件由两部分组成：结构（事件类型/参数）与表现（素材包提供的文案/插图插槽）。事件引擎不关心表现，素材包不关心逻辑。

## 物品（item）
用户拥有的物件（浆果/枯叶/石子/蒲公英…），存在用户的物品栏中。**MVP 来源：用户当天首次登录，系统奖励一件**（类似旅行青蛙里老妈给仔仔备便当）。未来：引入货币体系后可在市场购买。物品是投喂的材料。

## 馈赠/便当（gift/bento）
用户主动将物品栏中的一件物品放在桌上送给 pet（= 投喂/签到）。每天限一次，错过不惩罚（佛系纪律）。pet 收下后可能：收进展物罐、在回信中提及、未来带出门旅行。对应旅行青蛙的「便当」机制。

## 回信（reply letter）
pet 对馈赠的回应：次日或当晚，pet 写一封信（信纸背景 + 回应文案，可引用所收物品与历史记忆）。低频但可预期，是「每日一小仪式」的闭环收尾。信的产生也是事件引擎的一种事件类型。

## 货币/市场（currency/market）【未来】
物品的获取途径将由「每日登录奖励」升级为「货币购买」。数据模型需预留物品的价格字段与货币账户，MVP 不实现。

## 中心自治邮件（hub-autonomous email）
注册/验证码邮件由**用户所注册的服务中心自己配置的 SMTP** 发出（每个中心自治配置，官方中心用官方邮箱）。由此推导：官方无法直接触达非本中心注册的用户——这是去中心化的本意（用户属于所注册的中心）。三件配套机制：① 中心元信息必须公开（中心名/域名/管理员邮箱/隐私承诺）② 官方公告全网可访问、可被各中心转发 ③ 邮件正文带中心身份标识（透明 + 防钓鱼）。

## UI 素材（asset）
表现素材包的可视化组成部分：家的背景图、信纸背景、家具元素（桌/床/窗/储物罐）、pet 状态图等。素材由设计者用外部工具（如即梦）按素材包规范产出，存放于素材包目录，与事件结构完全解耦。

### 事件流（event timeline）
pet 产生事件的单向时间线展示，宠物单向输出、无需用户回复，体现「无社交压力」。

## 已决议语义
- 产品形态：Web 应用；「跨平台」被视为不同外壳（壳），MVP 仅做 Web。
- 生命周期：MVP = 补算；远期 = 常驻进程（线上服务器）。
- 部署形态：必须存在可访问的服务端（官方托管中心为首发），数据不落前端。
- 账号体系：MVP 起就有账号（邮箱验证码），支持多 pet（结构一对多）。
- 用户触达：常规 = 应用内公告；极端必要 = 邮箱推送（极端谨慎，白名单限定）。
- 模式命名：面向用户统一用「隐居（hermit）」表述，不再用「单机」。
- pet 主动触达频率：遵守退避间隔表。